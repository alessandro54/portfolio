{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "preview": "turbo preview",
    "sanity:deploy": "pnpm --filter @portfolio/studio run deploy",
    "sanity:schema:deploy": "pnpm --filter @portfolio/studio run schema:deploy",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "turbo": "^2"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22.12.0"
  }
}
